module.exports = {
    siteUrl: `https://happy-smoke-0c765310f.azurestaticapps.net`, // Site domain. Do not include a trailing slash!

    postsPerPage: 12, // Number of posts shown on paginated pages (changes this requires sometimes to delete the cache)

    siteTitleMeta: `Atrispina in Azure`, // This allows an alternative site title for meta data for pages.
    siteDescriptionMeta: `A Gatsby frontend of the Atrispina.Stream Ghost blog housed in Azure Static Web Apps.`, // This allows an alternative site description for meta data for pages.

    shareImageWidth: 1000, // Change to the width of your default share image
    shareImageHeight: 523, // Change to the height of your default share image

    shortTitle: `AzureAtrispina`, // Used for App manifest e.g. Mobile Home Screen
    siteIcon: `favicon.png`, // Logo in /static dir used for SEO, RSS, and App manifest
    backgroundColor: `#e9e9e9`, // Used for Offline Manifest
    themeColor: `#15171A`, // Used for Offline Manifest
}
